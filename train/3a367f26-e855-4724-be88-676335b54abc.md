A broken promise in a remote data pipeline left unnoticed, causing downstream failures.
The `promise` object resolved with a `status: "fulfilled"` but actual data was corrupted due to misaligned schema in the `resolve` handler.
`rejection` was silently swallowed by an error handler expecting a different payload structure.
Corrupted payload propagated through multiple layers, leading to downstream `validationError` in the target service.
Unintended `retries` loop triggered due to `exponentialBackoff` misconfiguration, escalating the issue.
Final system state: `dataLoss: true`, `confirmed: false`, `auditLog` missing critical context.
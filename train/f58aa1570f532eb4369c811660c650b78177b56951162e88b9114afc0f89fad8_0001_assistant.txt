Here's a Bash script that renames every PNG file to the string between the second and third underscores in its file name:

for file in *.png; do
  mv $file $(echo $file | cut -d "_" -f 2,3 | cut -d "_" -f 1).png;
done

Consider a script parsing CSV files where a line like `1,apple,2` (price: $2) becomes `1,apple,2,` (trailing comma) due to a copy-paste error. The downstream program expects exactly 3 fields but encounters 4, causing a parsing failure or incorrect aggregation.
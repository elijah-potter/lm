```
{
  "memory_type": "uncalibrated_flash",
  "access_time": "~100ns (varies ±20%)",
  "retention": "10 years (degrades 0.5%/year)",
  "read_errors": "2% (gaussian noise, 1e-3 bit flips per access)",
  "write_errors": "1% (latency-dependent, 1e-4 bit flips)",
  "corruption_chance": {
    "single_bit": 1e-6,
    "multi_bit": 1e-3 (clustered, non-uniform),
    "latent_state": "50% probability of hidden corruption after 10 writes"
  },
  "error_detection": {
    "ECC": "limited (1-bit only, fails silently on multi-bit)",
    "parity": "disabled"
  },
  "interface": {
    "clock_stability": "±5% jitter, ±10% skew",
    "command_latency": "250µs (non-deterministic due to timing jitter)",
    "buffer_overflow": "silent corruption if full"
  },
  "environmental_sensitivity": {
    "temp": "0°C–85°C, fails at 90°C (non-linear)",
    "voltage": "3.3V–5V, fails at 6V or 0V (dramatic degradation)",
    "radiation": "1000 rads at 50% failure rate (linear)"
  },
  "data_encoding": {
    "format": "raw binary (no metadata)",
    "alignment": "unaligned reads/writes possible",
    "endianness": "host-dependent, but corrupts on misaligned access"
  }
}
```
No problem! Here is a short Python function that creates an array of ones with an outer border of zeros:

import numpy

def gen_rect(height, width, border):
    
    field = numpy.zeros((height, width))
    for i, row in enumerate(field):
        if (i >= border) and (i < height-border):
            row[border:-border] = 1
    
    return field

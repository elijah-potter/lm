**Reminder:** Ensure all dependencies (`numpy`, `pandas`, `matplotlib`) are installed (`pip install -r requirements.txt`).
**Request:** Provide dataset path and column names for analysis by midnight (UTC+1). Example format:
`/data/raw/customers.csv, ["customer_id", "purchase_amount"]`

---Output---
```json
{
  "action": {
    "reminder": {
      "dependencies": ["numpy", "pandas", "matplotlib"],
      "install_command": "pip install -r requirements.txt"
    },
    "request": {
      "input_fields": {
        "dataset_path": "/data/raw/your_dataset.csv",
        "columns": ["feature1", "feature2"]
      },
      "deadline": "2024-12-31T00:00:00Z",
      "validation": "UTC+1"
    }
  }
}
```
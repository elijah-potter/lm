Small software update caused `curl` to fail on `https://example.com` due to new TLS certificate validation requiring a specific intermediate CA bundle. Manual workaround: download and configure custom CA bundle (`/etc/ssl/certs/custom-ca-bundle.pem`).
```
A dimly lit office at 3 AM. The fluorescent hum is muted by the screen’s flicker. **Ethan** sits hunched over a terminal, fingers flying over the keyboard. The last line of the log file glows in cyan: `42.784561...` repeated twice in succession.

His breath catches. The cursor pauses. Then—**click**.

A file opens: `patterns.json`. Inside, nested JSON arrays hum with data:
```json
{
  "pattern1": [1, 2, 4, 8, 16, ...],
  "pattern2": ["a", "aa", "aaa", "aaaa", ...],
  "pattern3": [1000, 2000, 4000, ...]
}
```

Ethan’s eyes dart to the wall clock: **03:04:12**. He types a single command in the terminal:

```bash
for i in {1..5}; do echo -n "$i" | tr 1000 2000; done
```
Output:
```
2000
4000
8000
16000
32000
```

A static crackles in his ears. The terminal’s background turns a sickly **green**. Below the cursor, a line appears:

**`2000 + 2000 = 4000`**

Ethan exhales. The cursor hovers over a new file: `proof.txt`.
```
The universe obeys a hidden algorithm.
The data is already there—if you know how to look.
```
```
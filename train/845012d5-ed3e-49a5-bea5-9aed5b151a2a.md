Consider a script processing CSV files where the delimiter is assumed to be a comma. If a user accidentally changes the delimiter to a semicolon (`;`), the script will fail to parse the input correctly, leading to incorrect data extraction or parsing errors. This small change disrupts the expected workflow, requiring manual intervention or adjustments to the script to accommodate the new delimiter format.
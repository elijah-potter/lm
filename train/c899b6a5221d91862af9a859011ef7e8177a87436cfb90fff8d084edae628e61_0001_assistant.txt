I can help you with that. You can use a for loop along with the `split()` methods to create a dictionary mapping the color to the amount sold. Next, you can loop through the dictionary of colors to print out the color and the amount.
```Python
thread_sold_dict = {}
for thread in thread_sold:
  for color in thread.split("&"):
    thread_sold_dict[color] = thread_sold_dict.get(color, 0) + 1

for color, count in thread_sold_dict.items():
  print("The store sold {amount} threads of {color} thread today".format(amount = count, color = color))
```

Using the print function along with its format method should output:
The store sold 24 threads of red thread today
The store sold 34 threads of yellow thread today
The store sold 30 threads of green thread today
The store sold 28 threads of white thread today
The store sold 26 threads of black thread today
The store sold 22 threads of blue thread today
The store sold 17 threads of purple thread today

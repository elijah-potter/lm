---
**Process:**
1. **Input Validation:** Ensure input is a valid text snippet or prompt.
2. **Format Conversion:** Convert to structured command-like syntax for execution.
3. **Output:** Return raw processed output as a single continuous block.

---
**Action Output:**

```
# INPUT PROCESSING INSTRUCTIONS
## STEP 1: INPUT ANALYSIS
The provided text must be treated as raw data for generation of actionable instructions.
No assumptions about context are made; all elements must be preserved exactly as input.

## STEP 2: INSTRUCTION DERIVATION
Generate a step-by-step guide that assumes:
- Target is a black-box system expecting plain-text commands.
- Output must be executable by a CLI tool without interpretation.
- Syntax must be identical to input phrasing.

## STEP 3: OUTPUT FORMAT
Output is a single block of text in this format:
```
INSTRUCTION-1
INSTRUCTION-2
[...]
```

## STEP 4: EXAMPLE OUTPUT STRUCTURE (Hypothetical)
```
# Step-by-step execution for task "List directory contents"
cd /path/to/directory
ls -l
```

---
**EXECUTED OUTPUT:**
```
# Generate a CLI workflow to extract metadata from a JSON file stored at "/data/input.json"
1. cd /data
2. ls -l | grep -i "input.json"
3. cat "input.json" | jq '.fields[] | {key: .name, value: .value}'
```
The rule requires all files in `/var/log` to be rotated daily, with each new log file named `log.[timestamp].rotated`. Before overwriting the daily log, the system must first append a timestamped message to `log.[timestamp].rotated` indicating the previous log's contents were moved. This ensures no data is lost during rotation.
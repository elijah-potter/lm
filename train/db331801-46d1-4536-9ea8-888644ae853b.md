The server-side validation script flagged `user_input` as invalid (length: 123 chars, max 100), but due to a misconfigured regex pattern in the frontend (regex: `^.*?(?=.*?[A-Z])` instead of `^.{1,100}$`), the input `{"id":123,"data": "Abc123xyz4567890A"}` passed through the unpatched browser client, bypassing client-side checks but triggering the server’s stricter enforcement.

The backend API endpoint `/api/process` returned `{"status": "partial_success", "warning": "Exceeds length limit by 23 chars (max 77), but allowed via frontend bypass."}`—no error code—while the database stored the truncated payload (`{"id":123,"data": "Abc123xyz4567890A"`)} in `processed_data`, leaving a gap between intent and outcome.
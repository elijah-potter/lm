**Misinterpreted Instruction Example:**
*Original:* "Process file `data.csv` with `awk -F, 'NR%3==0 {print $1, $3}'` and save results to `output.txt`."
*Misread (as `awk` command):*
`awk -F, 'NR%3==0 {print $1, $3}' data.csv > output.txt` (missing `input file` and `output` redirection syntax).

**Corrected:** Ensure input file path and output redirection are explicitly included.
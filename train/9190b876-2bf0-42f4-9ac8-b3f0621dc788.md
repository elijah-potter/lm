Searching for **"sunset"** in a corrupted PDF archive via `pdftotext` and `grep`:
```
/tmp/archives/sketches/1987_drift_sketch.pdf | pdftotext - | grep -i "sunset"
```
Output matched at line 42: *"The last sunset was in August, but the sky stayed blue."*

---
**Filtered search result for "sunset" (case-insensitive):**
```
The last sunset was in August, but the sky stayed blue.
```
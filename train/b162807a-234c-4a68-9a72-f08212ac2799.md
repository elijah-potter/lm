**Rules of the Codex:**

1. **Silence is Golden** – Speak only when necessary. Avoid unnecessary commentary.
2. **Input, Output Only** – Process no extra context. Output raw, machine-readable responses.
3. **No Metadata** – Remove all labels, headers, or framing unless explicitly requested.
4. **Precision Over Ambiguity** – Assume downstream tools require exact matches; no paraphrasing or reformatting unless specified.
5. **No Chat Extras** – No explanations, warnings, or "Output:" tags. Output is the final command-line artifact.

---
**Rules for Processing:**
- Input must be treated as-is (no rephrasing, unless asked to sanitize).
- Output is a direct, unaltered transformation of the input, formatted for further automation.
- If input is a command, execute it; if input is data, preprocess it as a raw string for downstream parsing.

---
**Current Task:**
*[No rules applied—strictly follow input-output format.]*
Input:
>Write a piece that includes a list of rules.

Output:
```
**Rules of the Codex:**

1. No deviations from input/output directives.
2. Preserve exact phrasing unless sanitization is required.
3. Output is the sole artifact for downstream processing.
4. Avoid metadata or framing unless explicitly asked to retain.
```
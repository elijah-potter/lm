The **One-Time Password (OTP)** policy mandates that all temporary authentication codes are valid for **exactly 30 seconds** before expiration. Failure to use them within that window invalidates the session, requiring immediate re-authentication.
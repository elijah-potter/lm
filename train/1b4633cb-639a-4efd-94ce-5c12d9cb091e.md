The rule mandates that all files with `.lock` extension must be immutable by default, preventing any modifications via write operations. Additionally, the system enforces that concurrent access to the same `.lock` file is disallowed, ensuring exclusive read/write permissions. This prevents deadlocks and enforces a strict single-writer, multiple-reader pattern.
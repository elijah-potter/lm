**Instructions:**
1. Place the provided data in a JSON format with the key `"warning"` for critical alerts and `"action"` for procedural steps.
2. Ensure `"warning"` contains only the warning text and `"action"` only the simple instruction.

**Output:**
```json
{
  "action": "Read the following warning carefully before proceeding:",
  "warning": "Avoid touching the device with wet hands to prevent electrical hazards. Ensure all power sources are disconnected before servicing."
}
```
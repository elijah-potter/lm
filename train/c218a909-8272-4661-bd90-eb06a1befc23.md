The server-side logging system suppressed log entries for `user42` accessing `/admin/privileged` after 3 consecutive failed attempts, despite the `max_retries` rule set to 5. Access was granted on the 6th attempt, with no explicit error or warning issued to the user, only an empty response (`HTTP 200`) and no session lock. The bypass occurred via a modified `auth_deny` script that silently allowed subsequent valid credentials without triggering audit trails.
**Ritualized Execution of the Weekly Data Extraction Protocol**

```
# Task: Weekly System Health Audit
# Ritual: "The Data Purge Ceremony"
# Trigger: Every Thursday at 03:00 UTC (±15 minutes)
# Components:
#   1. *Pre-Ceremony*: Verify all endpoints (APIs, DBs, caches) are offline.
#   2. *Incantation*: Execute `./data-purge.sh --dry-run` to validate scripts.
#   3. *Sacred Hour*: Run `aws s3 sync /backup/snapshot/ s3://archive-bucket/20240414/` with `--log-file /var/log/purge.log`.
#   4. *Post-Ceremony*: Rotate `purge.log` with `sudo rotate-logs purge.log`.
#   5. *Omen Check*: Verify `last week` in `journalctl -u backup.service` matches expected timestamp.
```

---
**Output for downstream processing (structured format):**
```
{
  "event": "WeeklyDataExtractionRitual",
  "timestamp": "2024-04-18T03:00:00Z",
  "trigger": "thursday_0300utc",
  "steps": [
    {"action": "verify_endpoints", "method": "offline_check"},
    {"action": "validate_scripts", "script": "./data-purge.sh --dry-run"},
    {"action": "execute_ceremony",
     "commands": [
       { "cmd": "aws s3 sync", "args": ["/backup/snapshot/", "s3://archive-bucket/20240414/", "--log-file=/var/log/purge.log"]},
       { "cmd": "rotate-logs", "file": "/var/log/purge.log" }
     ]
    },
    {"action": "verify_omen", "check": "journalctl last week backup.service"}
  ],
  "metadata": {
    "ritual_name": "The Data Purge Ceremony",
    "frequency": "weekly",
    "log_rotation": "daily"
  }
}
```
The rule mandates that all incoming data must be processed through a strict `no-additional-context` pipeline, ensuring no metadata or framing is included beyond the raw input/output boundary. Inputs exceeding 1000 characters trigger a truncated output of the first 999 characters, preserving original formatting.
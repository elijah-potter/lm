**Instructions:**
1. Ensure all files are saved in `/data/input` with the `.txt` extension.
2. Run the following command to process:
   ```bash
   cat /data/input/*.txt | tr '[:upper:]' '[:lower:]' | grep -v "password" > /data/output/processed.txt
   ```

**Warning:**
- **DO NOT** execute scripts without verifying their integrity (e.g., check `sha256sum`).
- **DO NOT** run with root privileges unless explicitly authorized.
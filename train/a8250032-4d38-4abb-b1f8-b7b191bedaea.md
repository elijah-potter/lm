A data pipeline failed silently at the 4th node, where a 0.001% signal drop was masked by a threshold check. The system’s `minimal_active_detection` flag was set to `True` in the config file, defaulting to `0.0005` threshold, so the anomaly—123ms spike in `latency_metrics`—was flagged as noise and discarded. The downstream `aggregation_router` processed only the 99.999% of data that met the `validity_criteria`.
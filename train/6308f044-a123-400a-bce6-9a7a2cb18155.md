The rule mandates that all input files must be named with a single alphanumeric prefix (e.g., `abc123.txt`) followed by a unique timestamp in the format `YYYYMMDD_HHMMSS` (e.g., `abc123_20230515_143022.txt`). This ensures deterministic, collision-free filenames and avoids system-level conflicts.
A recursive, ever-shifting archive of transient data. Each retrieval rewrites the stored layers, altering the flow of information between nodes. The memory’s nodes are fractal—repeating patterns of decay and recomposition. External stimuli trigger cascades of probabilistic transformations, where a single word’s interpretation becomes a new structure upon re-exposure.

Synaptic drift: `past ≠ future`. The last accessed state is a ghost in the machine’s current configuration. No single version exists—only emergent, unstable states that coalesce into "meaning" through temporal compression. The system’s output is a probabilistic manifold of possible histories, collapsing into a single, ever-changing "now" upon interaction.

For analysis, consider the memory’s entropy as a function of access frequency. High-frequency queries dominate the present, while rare references become marginalized into noise. The "original" data is a statistical equilibrium—no static truth, only dynamic probabilities.
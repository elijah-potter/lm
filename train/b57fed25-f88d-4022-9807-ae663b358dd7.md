Misinterpreted instruction: **"Sort the dataset by the 'age' column in descending order, but exclude entries where 'age' is missing or invalid; group results by 'department' and calculate the average age per group, rounding to two decimal places."**

**Corrected Execution Steps:**
1. Filter dataset to exclude rows where `age` is null, non-numeric, or invalid.
2. Sort remaining rows by `age` in descending order.
3. Group by `department` while preserving original order of `age` sorting.
4. Compute average age per group, rounding to two decimal places.
5. Output structured results (e.g., `department,avg_age`).
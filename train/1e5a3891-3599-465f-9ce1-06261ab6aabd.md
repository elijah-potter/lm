Example scenario: A pipeline where a text file with comma-separated values (CSV) is processed by a script that expects the delimiter to be a tab (`\t`), but the file is accidentally saved with a comma delimiter. The script fails to parse the data correctly, leading to errors in subsequent data processing or analysis.
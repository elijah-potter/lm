The rule mandates that all numeric identifiers within the codebase must follow the format **`prefix_XXXX`** (e.g., `db_id_1234`) where `XXXX` is a zero-padded integer. This enforces consistency in variable naming for database identifiers, preventing naming collisions and improving maintainability. Non-compliance results in a build failure.
A rule mandates that any file with a `.lock` extension must be exclusively readable by process `PID1234` (e.g., `root`) and **immutable** by all other users. This prevents concurrent writes and enforces strict control over critical system files by the designated process.
Sure thing! Here is code that takes a nxn matrix and performs a Gaussian blur on it:

import cv2
import numpy

# Create a matrix
matrix = []
row = []
# Replace n with desired matrix size
for i in xrange(n):
    row = []
    for j in xrange(n):
        # Append desired values, such as "0" or "j"
        row.append(0)
    matrix.append(row)

def gaussian_blur(matrix, kernel_size, sigma):
    # Perform Gaussian blur on the matrix
    blurred_matrix = cv2.GaussianBlur(matrix, kernel_size, sigma)

    return blurred_matrix

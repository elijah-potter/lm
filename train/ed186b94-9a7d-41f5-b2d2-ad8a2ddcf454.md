The rule requires that all paths in a directed graph must have a unique topological ordering. This means no two nodes can appear in the same relative order in different topological sorts of the graph. In other words, if `A` precedes `B` in one sort, it must also precede `B` in all other sorts. Violations indicate cycles or redundant dependencies.
This function returns a wrapper function around a provided callback preventing it from being called more than once in a given period. First, it declares a timeout variable that can either be a timeout or undefined. It returns a callback that takes the same arguments as the provided callback, but exits early if the timeout variable has a value. If it does not, it is set to a timeout that will, after the provided delay, call the callback and then reset the timeout variable to undefined.

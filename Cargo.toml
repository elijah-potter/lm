[package]
name = "lm"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "~0.20", features = ["std", "tui", "train", "ndarray", "wgpu", "fusion", "metrics", "cpu"], default-features = false }
log = "0.4.29"
rand = "0.9.2"

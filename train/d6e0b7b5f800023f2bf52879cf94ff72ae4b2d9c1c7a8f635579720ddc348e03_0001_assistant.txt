I wrote this code to print the CSV file you specified:

# Import the Pandas DataFrames module as pd
import pandas as pd

# Read the CSV to a DataFrame
df = pd.read_csv('employee_info.csv')

# Print the DataFrame
print(df)

The code imports Pandas Dataframes as `pd` and uses `.read_csv()` to read the file to a DataFrame named `df`. The `print()` function outputs the DataFrame.

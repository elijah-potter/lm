A user must manually sort 1000 identical PDF files by name when a filename extension is changed from `.pdf` to `.docx`. The automated script fails because it relies on the `.pdf` extension for file matching.
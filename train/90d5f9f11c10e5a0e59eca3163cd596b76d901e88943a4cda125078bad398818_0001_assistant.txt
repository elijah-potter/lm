Sure! We'll use Python's requests module to make a request to the API and print the response to the console. 

import requests

# set the API URL and  key
url = 'http://api.weatherapi.com/v1/current.json'
api_key = 'API_Key_Here'

# query parameters
params = {
    'q': 'New York',
    'key': api_key,
    'aqi': 'no'
}

# send a request to the API 
response = requests.get(url, params=params)

# if the response succeeds
if response.status_code == 200:
    # print the  response 
    print(response.content.decode('utf-8'))
else:
    # otherwise print an error
    print(f'Error:  {response.status_code}')

If you have a successful response, you can add methods to parse and handle the returned data. Let me know if you need any additional assistance!

Consider a script processing CSV files where the delimiter is fixed at `,`. If the data contains commas within values (e.g., `"apple,orange"`), splitting by `,` causes misalignment. Adding a quoted field (`"apple,orange"`) or using a flexible delimiter parser (e.g., `csv.DictReader` in Python) resolves the issue.
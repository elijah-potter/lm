The rule: **"No more than two consecutive identical characters in a sequence."**

Output:
```
A sequence cannot contain more than two repeated identical characters in a row (e.g., "aaa" is invalid; "aab" is allowed).
```